// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl"]
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:fDAaTfFRQPj3vTu7d3@musicloud-postgres-database.crg8aqzznzci.us-east-1.rds.amazonaws.com:5432/musicloud?schema=public"
}

model User {
  id       Int           @id @default(autoincrement())
  name     String
  email    String        @unique
  password String?
  provider AuthProviders

  @@map("users")
}

enum AuthProviders {
  EMAIL
  GITHUB
}
